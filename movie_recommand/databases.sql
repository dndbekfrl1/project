create database Movie;
use Movie;

create table Megabox(
	title char(30),
    review text,
    point char(30)
);

insert into Megabox(title,review,point) values(1,1,1);
select * from megabox;


create database CGV;
use CGV;

create table CGV(
	title char(30),
    review text,
    egg char(30)
);

insert into CGV(title,review,egg) values(1,1,1);
select * from CGV;
